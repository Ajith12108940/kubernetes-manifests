apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: vegetable-route

spec:
  parentRefs:
  - name: vegetable-gateway

  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /

    backendRefs:
    - name: vegetable-service
      port: 80